import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.albaqiyat.alsalihat',
  appName: 'الباقيات الصالحات',
  webDir: 'dist'
};

export default config;
