/**
 * UIOverlay — تم إلغاء هذه الطبقة نهائياً.
 * كانت تنشئ شريطاً علوياً خارجياً ("الباقيات الصالحات" + زر "تجربة الاحتفال")
 * يُغطي أيقونات اللعبة الداخلية. أُزيل الشريط بالكامل ولم يعد يُستخدم
 * (حُذف استدعاء initOverlay من main.ts). الدالة أصبحت لا تقوم بأي شيء.
 */
export function initOverlay(): void {
  // لا شيء — الشريط الخارجي ملغى. keep empty.
}
